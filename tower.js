
class Tower {
  constructor(x, y, width, height) {
    this.body = Bodies.rectangle(x, y, width, height);
    this.width = width;
    this.height = height;
     }
  display(){
    rectMode(CENTER);
    this.shapeColor="pink"
    
  
  }
};
